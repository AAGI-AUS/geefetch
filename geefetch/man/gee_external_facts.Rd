% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external_facts.R
\name{gee_external_facts}
\alias{gee_external_facts}
\title{External-fact provenance registry}
\usage{
gee_external_facts(grounded_only = FALSE)
}
\arguments{
\item{grounded_only}{Logical. If \code{TRUE}, return only facts with a non-\code{NA}
\code{verified_on}. Default \code{FALSE} (return all, including \verb{[unverified]}).}
}
\value{
A \link[data.table:data.table]{data.table::data.table}, one row per externally-grounded fact,
with columns:
\describe{
\item{dataset}{Normalised dataset ID.}
\item{fact_id}{\verb{<dataset>.<fact>} identifier.}
\item{kind}{Fact kind: \code{locator}, \code{enum}, \code{scale}, or \code{bound}.}
\item{value}{The value the package asserts (read live from the registry).}
\item{source}{The authority that owns the fact (a GEE STAC catalogue URL).}
\item{verified_on}{ISO date last diffed against \code{source}, or \code{NA}.}
\item{grounding}{\code{"grounded"} or \code{"[unverified]"}.}
\item{oracle_types}{Which oracle(s) ground (or would ground) the fact.}
\item{reverify_by}{\code{"stable"} or \code{"volatile"}.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Enumerates every fact \code{geefetch} asserts about Google Earth Engine -- each
dataset's collection (asset) ID, scale factor, offset, band set, and start
date -- alongside the external authority that owns the fact and the date the
fact was last diffed against that authority. A fact whose \code{verified_on} is
\code{NA} is reported as \verb{[unverified]}: a value the package asserts but has \strong{not}
grounded against an independent oracle, and which must be treated as a
hypothesis rather than a fact.

The fact values are read live from the internal registry, so this table can
never drift from the code it documents. Grounding is established by the
independent-oracle tests (catalogue diff against the public GEE STAC, and an
optional live-contract check); see the package tests.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
gee_external_facts()
gee_external_facts(grounded_only = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=gee_datasets]{gee_datasets()}}

Other utilities:
\code{\link[=gee_clear_cache]{gee_clear_cache()}},
\code{\link[=gee_datasets]{gee_datasets()}},
\code{\link[=gee_register_dataset]{gee_register_dataset()}}
}
\concept{utilities}
