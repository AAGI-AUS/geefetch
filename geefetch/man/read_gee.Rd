% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_gee.R
\name{read_gee}
\alias{read_gee}
\title{Read data from Google Earth Engine}
\usage{
read_gee(
  dataset_id,
  ...,
  backend = c("rest", "rgee"),
  cache = TRUE,
  max_tries = 3L,
  initial_delay = 1L
)
}
\arguments{
\item{dataset_id}{Character. Dataset name or alias. Use \code{\link[=gee_datasets]{gee_datasets()}}
to list all available datasets.}

\item{...}{Arguments passed to the dataset-specific handler. Common
arguments include \code{date}, \code{region}, \code{collection}, \code{depth}. See the
convenience function documentation for dataset-specific parameters.}

\item{backend}{Character. \code{"rest"} (default) or \code{"rgee"}. The REST backend
requires no Python; the rgee backend supports advanced server-side
computations.}

\item{cache}{Logical. Use disk cache for repeated queries? Default \code{TRUE}.}

\item{max_tries}{Integer. Maximum retry attempts for network failures.
Default \code{3L}.}

\item{initial_delay}{Numeric. Initial delay in seconds before retry
(doubles each attempt). Default \code{1}.}
}
\value{
A \code{\link[terra:rast]{terra::rast()}} SpatRaster object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Central dispatcher for all GEE dataset reads. Accepts a dataset identifier
(name or alias) and delegates to the appropriate internal handler.
For most use cases, prefer the convenience functions (e.g.,
\code{\link[=read_modis_ndvi]{read_modis_ndvi()}}, \code{\link[=read_era5]{read_era5()}}) which provide named parameters
and dataset-specific documentation.
}
\section{Supported datasets}{

Use \code{\link[=gee_datasets]{gee_datasets()}} to list all available datasets with metadata.

\strong{Tier 1 — Specialised handlers (QA masking):}
\itemize{
\item \code{modis_ndvi} — MODIS Terra NDVI 16-day 1km
\item \code{modis_lst} — MODIS Terra LST 8-day 1km
\item \code{era5_temp} — ERA5-Land daily 2m temperature
\item \code{era5_precip} — ERA5-Land daily precipitation
\item \code{chirps_precip} — CHIRPS daily precipitation
\item \code{srtm_elevation} — NASA SRTM 30m elevation (static)
}

\strong{Tier 2 — Specialised handlers (soil, computed indices):}
\itemize{
\item \code{slga_cly}, \code{slga_snd}, \code{slga_slt}, \code{slga_awc}, \code{slga_bdw},
\code{slga_phc}, \code{slga_nto} — SLGA soil properties (Australia, static)
\item \code{sentinel2_ndvi} — Sentinel-2 NDVI 10m (computed, cloud-masked)
\item \code{landsat_ndvi} — Landsat 9 NDVI 30m (computed, cloud-masked)
}

\strong{Tier 3 — Generic handler (global datasets):}
\itemize{
\item \code{worldclim_bio} — WorldClim V1 bioclimatic variables ~1km (static)
\item \code{openlandmap_soc} — OpenLandMap soil organic carbon 250m (static)
\item \code{openlandmap_clay} — OpenLandMap clay content 250m (static)
\item \code{openlandmap_ph} — OpenLandMap soil pH 250m (static)
}

\strong{User-registered:} Any dataset added via \code{\link[=gee_register_dataset]{gee_register_dataset()}}
is automatically dispatchable through the generic handler.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Using the dispatcher directly
ndvi <- read_gee("MODIS_NDVI", date = "2024-06-15",
                 region = terra::ext(138, 140, -36, -34))

# Aliases are case-insensitive
elev <- read_gee("srtm", region = terra::ext(138, 140, -36, -34))

# Equivalent convenience function (preferred)
ndvi <- read_modis_ndvi(date = "2024-06-15",
                        region = terra::ext(138, 140, -36, -34))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=collect_gee_data]{collect_gee_data()}} for batch point extraction returning a
\link[data.table:data.table]{data.table::data.table}.

Other GEE readers: 
\code{\link{read_chirps}()},
\code{\link{read_era5}()},
\code{\link{read_landsat}()},
\code{\link{read_modis_lst}()},
\code{\link{read_modis_ndvi}()},
\code{\link{read_sentinel2}()},
\code{\link{read_slga}()},
\code{\link{read_srtm}()},
\code{\link{read_worldclim}()}
}
\concept{GEE readers}
