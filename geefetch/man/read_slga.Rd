% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_slga.R
\name{read_slga}
\alias{read_slga}
\title{Read SLGA soil data from Google Earth Engine}
\usage{
read_slga(
  region,
  collection = c("CLY", "SND", "SLT", "AWC", "BDW", "PHC", "NTO"),
  depth = "0-5",
  stat = c("mean", "ci_lower", "ci_upper"),
  backend = c("rest", "rgee"),
  cache = TRUE,
  max_tries = 3L,
  initial_delay = 1
)
}
\arguments{
\item{region}{An \link[sf:sf]{sf::sf}, \code{\link[sf:sfc]{sf::st_sfc()}}, or \code{\link[terra:ext]{terra::ext()}} object
defining the spatial extent. Required.}

\item{collection}{Character. Soil attribute to extract. One of:
\itemize{
\item \code{"CLY"} — Clay content (percent)
\item \code{"SND"} — Sand content (percent)
\item \code{"SLT"} — Silt content (percent)
\item \code{"AWC"} — Available water capacity (mm)
\item \code{"BDW"} — Bulk density (g/cm3)
\item \code{"PHC"} — pH (CaCl2)
\item \code{"NTO"} — Total nitrogen (percent)
}

Default \code{"CLY"}.}

\item{depth}{Character. Depth layer. One of \code{"0-5"}, \code{"5-15"},
\code{"15-30"}, \code{"30-60"}, \code{"60-100"}, \code{"100-200"}. Default \code{"0-5"}.}

\item{stat}{Character. Statistic. \code{"mean"} (default), \code{"ci_lower"}
(5th percentile), or \code{"ci_upper"} (95th percentile).}

\item{backend}{Character. \code{"rest"} (default) or \code{"rgee"}.}

\item{cache}{Logical. Use disk cache? Default \code{TRUE}.}

\item{max_tries}{Integer. Retry attempts. Default \code{3L}.}

\item{initial_delay}{Numeric. Initial retry delay in seconds. Default \code{1}.}
}
\value{
A \code{\link[terra:rast]{terra::rast()}} SpatRaster. CRS: EPSG:4326. Values depend
on the collection (e.g., percent for clay, pH units for PHC).
}
\description{
Reads CSIRO Soil and Landscape Grid of Australia (SLGA) data from
Google Earth Engine. Supports 7 soil attributes, 6 depth layers,
and 3 statistics. This is a static dataset (no date required).
}
\section{Data availability}{

Static dataset. Australia only. 90 m spatial resolution.
6 depth layers: 0-5, 5-15, 15-30, 30-60, 60-100, 100-200 cm.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Clay content, 0-5cm, mean
clay <- read_slga(region = terra::ext(138, 140, -36, -34))

# pH at 30-60cm depth
ph <- read_slga(region = terra::ext(138, 140, -36, -34),
                collection = "PHC", depth = "30-60")

# Sand content with confidence interval
sand_lo <- read_slga(region = terra::ext(138, 140, -36, -34),
                     collection = "SND", stat = "ci_lower")
\dontshow{\}) # examplesIf}
}
\references{
Viscarra Rossel, R.A. et al. (2015). A digital soil map of Australia.
Soil Research, 53(7), 745-757. \doi{10.1071/SR15158}
}
\seealso{
\code{\link[=read_gee]{read_gee()}}, \code{\link[=collect_gee_data]{collect_gee_data()}}

Other GEE readers: 
\code{\link{read_chirps}()},
\code{\link{read_era5}()},
\code{\link{read_gee}()},
\code{\link{read_landsat}()},
\code{\link{read_modis_lst}()},
\code{\link{read_modis_ndvi}()},
\code{\link{read_sentinel2}()},
\code{\link{read_srtm}()},
\code{\link{read_worldclim}()}
}
\concept{GEE readers}
