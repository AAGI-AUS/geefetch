% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handler_registry.R
\name{gee_register_dataset}
\alias{gee_register_dataset}
\title{Register a custom GEE dataset}
\usage{
gee_register_dataset(
  name,
  collection,
  bands,
  scale,
  temporal,
  description,
  domain = "User-defined",
  qa_band = NA_character_,
  scale_factor = 1,
  offset = 0,
  citation = NA_character_
)
}
\arguments{
\item{name}{Character. Short, unique identifier for the dataset
(e.g., \code{"soilgrids_ocs"}). Will be normalised to lowercase.}

\item{collection}{Character. GEE ImageCollection ID
(e.g., \code{"projects/soilgrids-isric/ocs"}).}

\item{bands}{Character vector. Band name(s) to extract.}

\item{scale}{Integer. Native resolution in metres.}

\item{temporal}{Character. Temporal resolution: \code{"daily"}, \code{"8day"},
\code{"16day"}, \code{"monthly"}, \code{"5day"}, or \code{"static"}.}

\item{description}{Character. Human-readable dataset description.}

\item{domain}{Character. Scientific domain (e.g., \code{"Soil"},
\code{"Vegetation"}). Default \code{"User-defined"}.}

\item{qa_band}{Character. QA band name, or \code{NA} if none.
Default \code{NA_character_}.}

\item{scale_factor}{Numeric. Multiply raw values by this factor.
Default \code{1}.}

\item{offset}{Numeric. Add this to values after scaling. Default \code{0}.}

\item{citation}{Character. Citation string. Default \code{NA_character_}.}
}
\value{
\code{NULL}, invisibly. Called for side effects (registration).
}
\description{
Adds a user-defined GEE collection to the geefetch registry for the
current R session. Once registered, the dataset can be used with
\code{\link[=read_gee]{read_gee()}} and \code{\link[=collect_gee_data]{collect_gee_data()}} like any built-in dataset.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
gee_register_dataset(
  name        = "soilgrids_ocs",
  collection  = "projects/soilgrids-isric/ocs",
  bands       = "ocs_0-30cm_mean",
  scale       = 250L,
  temporal    = "static",
  description = "SoilGrids Organic Carbon Stock 0-30cm"
)

# Now available in the registry
gee_datasets()
read_gee("soilgrids_ocs")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=gee_datasets]{gee_datasets()}}, \code{\link[=read_gee]{read_gee()}}

Other utilities: 
\code{\link{gee_clear_cache}()},
\code{\link{gee_datasets}()}
}
\concept{utilities}
