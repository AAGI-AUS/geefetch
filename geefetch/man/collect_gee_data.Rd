% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect_gee_data.R
\name{collect_gee_data}
\alias{collect_gee_data}
\title{Batch-extract GEE data at point locations}
\usage{
collect_gee_data(
  lon = NULL,
  lat = NULL,
  xy = NULL,
  date_range,
  datasets = NULL,
  depth = "0-5",
  stat = "mean",
  backend = c("rest", "rgee"),
  cache = TRUE,
  verbose = TRUE,
  na.rm = FALSE
)
}
\arguments{
\item{lon, lat}{Numeric vectors of coordinates (WGS84). Ignored if
\code{xy} is provided.}

\item{xy}{A data.frame, matrix, or \link[sf:sf]{sf::sf} object with point
locations. Must contain columns named \code{lon}/\code{lat}, \code{x}/\code{y},
or \code{longitude}/\code{latitude}, or be an sf POINT geometry.}

\item{date_range}{A length-2 character or Date vector (\code{c(start, end)}),
or an explicit vector of dates.}

\item{datasets}{Character vector of dataset names or aliases.
Use \code{\link[=gee_datasets]{gee_datasets()}} to list available options.}

\item{depth}{Character. For SLGA soil datasets: depth layer(s).
\code{"0-5"}, \code{"5-15"}, ..., \code{"100-200"}, or \code{"all"}. Default \code{"0-5"}.}

\item{stat}{Character. For SLGA soil datasets: statistic.
\code{"mean"}, \code{"ci_lower"}, \code{"ci_upper"}. Default \code{"mean"}.}

\item{backend}{Character. \code{"rest"} (default) or \code{"rgee"}.}

\item{cache}{Logical. Default \code{TRUE}.}

\item{verbose}{Logical. Print progress table and status messages?
Default \code{TRUE}.}

\item{na.rm}{Logical. Remove rows where all dataset columns are NA?
Default \code{FALSE}.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with columns:
\describe{
\item{date}{Date of extraction}
\item{lon, lat}{Coordinates (always included)}
\item{point_id}{Integer point identifier}
\item{\if{html}{\out{<dataset columns>}}}{One column per requested dataset}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Extracts values from multiple GEE datasets at one or more point
locations across a date range. Returns a single \link[data.table:data.table]{data.table::data.table}
with one row per location-date combination and one column per dataset.

This is the primary function for building environmental covariate
tables for modelling.
}
\section{Column naming}{

Each dataset produces a column named after its normalised ID
(e.g., \code{modis_ndvi}, \code{era5_temp}, \code{srtm_elevation}). Static
datasets have their value replicated across all dates.
}

\section{Error handling}{

Failed extractions for individual dataset-date combinations produce
\code{NA} values with a warning. Execution continues for remaining
datasets. This matches \code{nert}'s resilient batch behaviour.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Single location, multiple datasets
dt <- collect_gee_data(
  lon = 138.6, lat = -34.9,
  date_range = c("2024-01-01", "2024-03-31"),
  datasets = c("modis_ndvi", "era5_temp")
)

# Multiple locations from a data.frame
sites <- data.frame(lon = c(138.6, 149.1), lat = c(-34.9, -35.3))
dt <- collect_gee_data(
  xy = sites,
  date_range = c("2024-01-01", "2024-12-31"),
  datasets = c("modis_ndvi", "era5_temp", "srtm_elevation")
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=read_gee]{read_gee()}} for single-dataset reads,
\code{\link[=gee_datasets]{gee_datasets()}} to list available datasets.
}
\concept{GEE batch}
